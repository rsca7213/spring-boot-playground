package com.playground.api.enums;

public enum ErrorCode {
    ITEM_ALREADY_EXISTS,
    UNSUPPORTED_OPERATION,
    INVALID_FILTER,
    ITEM_DOES_NOT_EXIST,
    INVALID_PARAMETERS,
    RUNTIME_ERROR,
}
